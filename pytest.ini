[pytest]
testpaths = .
python_files = test_*.py
# Implicit: doar -q, ca sa mearga si fara pytest-cov (pip install pytest).
# Acoperire (dupa: pip install pytest-cov):
#   python -m pytest --cov=bank_account --cov-branch --cov-report=term-missing
addopts = -q
